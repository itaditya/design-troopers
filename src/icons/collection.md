We use the standard Material Icons. Press `CMD + F` to search icons.

```js noeditor
import { IconBox } from '../docs/doc_components';

const iconsList = require('../../iconsList.json');

<section>
  <IconBox iconsList={iconsList} />
</section>

```
